$(document).ready(function() {
$.ajaxSetup ({
    // Disable caching of AJAX responses */
    cache: false
});
	
	
$("#modalbox").fancybox({

        autoSize: false,
        height: 'auto',
        width: 'auto'
      
});//endfancybox

});//end ready function
